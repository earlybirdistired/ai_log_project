services:
  ai-log-analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_log_analyzer
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
