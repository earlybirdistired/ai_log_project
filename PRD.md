# AI 보안 로그 분석기

> 상세 제품 요구사항 정의서(Product Requirements Document)

## 1. 목표와 성공 조건

### 1.1 목표

사용자가 최대 100줄의 보안 로그를 입력하면 AI가 로그를 분석하여 **공격 유형**, **위험도**, **분석 설명**을 제공하는 단일 화면의 보안 로그 분석 기능을 구현한다.

### 1.2 성공 조건

- 정상 로그, 공격 의심 로그, 위험 공격 로그를 포함한 3개 이상의 테스트 로그에서 결과가 정상적으로 출력된다.
- 각 테스트 로그는 최대 100줄로 구성한다.
- 로그 입력부터 결과 확인까지 1분 이내에 완료된다.
- 공격 유형, 위험도, 분석 설명이 명확하게 구분되어 표시된다.
- 분석 결과를 초기화하고 여러 로그를 연속해서 분석할 수 있다.

## 2. 사용자 스토리

| ID | 사용자 스토리 |
| --- | --- |
| US-01 | 보안 입문자로서 최대 100줄의 보안 로그를 입력하고 싶다. 로그를 직접 분석하지 않고 AI 분석 결과를 확인하기 위해서다. |
| US-02 | 보안 입문자로서 로그의 공격 유형을 확인하고 싶다. 어떤 종류의 공격이 발생했는지 빠르게 파악하기 위해서다. |
| US-03 | 보안 입문자로서 로그의 위험도를 확인하고 싶다. 해당 로그가 얼마나 위험한 상황인지 판단하기 위해서다. |
| US-04 | 보안 입문자로서 AI가 분석한 이유를 확인하고 싶다. 분석 결과를 이해하고 보안 로그 분석 방법을 학습하기 위해서다. |
| US-05 | 보안 입문자로서 분석 완료 후 새로운 로그를 다시 분석하고 싶다. 여러 로그를 연속해서 확인하기 위해서다. |

## 3. 화면 명세

### 3.1 화면명

**AI 보안 로그 분석**

### 3.2 화면 구성

#### ① 서비스 안내 영역

- 서비스명: `AI 보안 로그 분석기`
- 설명: `보안 로그를 입력하면 AI가 공격 유형과 위험도를 분석합니다.`

#### ② 로그 입력 영역

- 여러 줄의 보안 로그를 입력할 수 있는 텍스트 입력 영역
- 최대 100줄까지 입력 가능
- 현재 입력된 줄 수 표시
  - 예: `현재 37 / 100줄`
- 로그 입력 예시를 Placeholder로 제공
- 100줄을 초과하면 추가 입력 제한

#### ③ 분석 실행 영역

- `로그 분석하기` 버튼
- `초기화` 버튼
- 로그가 입력되지 않은 경우 분석 버튼 실행 제한

#### ④ 분석 결과 영역

분석 완료 후 다음 정보를 표시한다.

- 공격 유형
  - SQL Injection
  - XSS
  - Brute Force
  - Path Traversal
  - 정상 요청
  - 판단 불가
- 위험도
  - 낮음
  - 중간
  - 높음
  - 치명적
- 분석 설명
  - AI가 공격 유형과 위험도를 판단한 이유를 짧고 이해하기 쉬운 문장으로 표시

#### ⑤ 분석 상태 영역

| 상태 | 표시 내용 |
| --- | --- |
| 분석 전 | 결과 영역에 기본 안내 표시 |
| 분석 중 | `로그를 분석하고 있습니다...` 표시 |
| 분석 완료 | 분석 결과 표시 |
| 분석 실패 | 오류 메시지 표시 |

### 3.3 사용자 흐름

```text
로그 입력 → 로그 분석하기 클릭 → AI 분석 → 결과 출력 → 새로운 로그 입력 또는 초기화
```

## 4. 기능 명세

### F-01. 보안 로그 입력

- 사용자가 텍스트 형태의 보안 로그를 입력할 수 있어야 한다.
- 최대 입력 범위는 100줄이다.
- 입력 중 현재 줄 수를 실시간으로 표시한다.

### F-02. 로그 분석

- 사용자가 `로그 분석하기` 버튼을 클릭하면 입력된 로그를 AI에 전달한다.
- AI는 다음 정보를 반환한다.
  - 공격 유형
  - 위험도
  - 분석 설명

### F-03. 공격 유형 판단

AI는 입력 로그를 분석하여 대표적인 공격 유형을 판단한다.

- SQL Injection
- XSS
- Brute Force
- Path Traversal
- 정상 요청
- 판단 불가

대표 유형에 해당하지 않는 경우 로그 내용을 바탕으로 가장 적절한 유형을 설명하거나 `판단 불가`로 처리한다.

### F-04. 위험도 판단

AI는 분석 결과를 다음 4단계 중 하나로 표시한다.

- 낮음
- 중간
- 높음
- 치명적

로그만으로 위험도를 명확하게 판단하기 어려운 경우 무리하게 높은 위험도로 판단하지 않고, 분석 설명에 불확실성을 표시한다.

### F-05. 분석 설명 제공

AI가 공격 유형과 위험도를 판단한 이유를 사용자가 이해할 수 있는 짧은 설명으로 제공한다.

### F-06. 결과 표시

AI 분석 결과를 화면에서 다음과 같이 구분하여 표시한다.

```text
공격 유형: SQL Injection
위험도: 높음
분석 설명: 로그인 요청에 SQL 구문으로 판단되는 비정상적인 입력이 포함되어 있어 인증 우회 공격 가능성이 있습니다.
```

### F-07. 초기화

- `초기화` 버튼을 클릭하면 입력된 로그와 분석 결과를 모두 제거한다.
- 입력 줄 수를 `0 / 100줄`로 변경한다.
- 화면을 초기 상태로 되돌린다.

### F-08. 분석 중 상태 처리

- AI 분석 요청 중에는 `로그 분석하기` 버튼을 비활성화한다.
- 분석 완료 또는 오류 발생 후 버튼을 다시 활성화한다.
- 동일한 분석 요청이 중복으로 전송되지 않도록 한다.

## 5. 예외 처리

### E-01. 로그를 입력하지 않은 경우

**상황:** 사용자가 로그를 입력하지 않고 `로그 분석하기`를 클릭한다.

**처리:**

- AI 분석 요청을 보내지 않는다.
- 입력 영역 근처에 안내 메시지를 표시한다.
- 사용자가 로그를 입력하면 다시 분석할 수 있도록 한다.

```text
분석할 로그를 입력해주세요.
```

### E-02. 100줄을 초과하는 경우

**상황:** 사용자가 100줄을 초과하는 로그를 입력한다.

**처리:**

- 100줄까지만 입력할 수 있도록 제한한다.
- 현재 줄 수를 `100 / 100줄`로 표시한다.
- 다음 안내 메시지를 표시한다.

```text
로그는 최대 100줄까지 입력할 수 있습니다.
```

### E-03. AI 분석 요청이 실패한 경우

**상황:** AI 서비스 호출에 실패하거나 응답을 받지 못한다.

**처리:**

- 기존 정상 결과를 새로운 잘못된 결과로 덮어쓰지 않는다.
- 분석 버튼을 다시 활성화하여 재시도할 수 있도록 한다.
- 다음 메시지를 표시한다.

```text
분석에 실패했습니다. 잠시 후 다시 시도해주세요.
```

### E-04. 네트워크 오류가 발생한 경우

**상황:** 인터넷 연결 또는 AI 서비스와의 통신 과정에서 오류가 발생한다.

**처리:**

- 분석 결과를 성공한 것으로 표시하지 않는다.
- 분석 버튼을 다시 활성화한다.
- 다음 메시지를 표시한다.

```text
네트워크 연결에 문제가 발생했습니다. 연결 상태를 확인한 후 다시 시도해주세요.
```

### E-05. AI가 예상하지 않은 형식으로 응답한 경우

**상황:** AI 응답에 공격 유형, 위험도 또는 분석 설명이 없거나 예상 형식과 다르다.

**처리:**

- 잘못된 데이터를 정상 분석 결과로 표시하지 않는다.
- 사용자가 다시 분석할 수 있도록 한다.
- 다음 메시지를 표시한다.

```text
분석 결과를 처리할 수 없습니다. 다시 시도해주세요.
```

### E-06. 정상적인 로그인 경우

**상황:** 입력 로그에서 명확한 공격 패턴이 발견되지 않는다.

**처리:**

- 공격 유형을 `정상 요청` 또는 `공격 의심 없음`으로 표시한다.
- 위험도를 `낮음`으로 표시한다.
- 분석 설명에 공격 패턴이 발견되지 않았음을 명시한다.

```text
공격 유형: 정상 요청
위험도: 낮음
분석 설명: 현재 입력된 로그에서는 대표적인 공격 패턴이 확인되지 않았습니다.
```

### E-07. 공격 여부를 판단하기 어려운 경우

**상황:** 로그 정보가 부족하거나 정상 요청과 공격 요청을 구분하기 어렵다.

**처리:**

- AI가 임의로 특정 공격이라고 확정하지 않는다.
- 공격 유형을 `판단 불가`로 표시한다.
- 분석 설명에 판단이 어려운 이유와 추가 확인 필요성을 표시한다.

```text
공격 유형: 판단 불가
위험도: 판단 불가
분석 설명: 현재 로그만으로는 공격 여부를 명확하게 판단하기 어렵습니다. 추가적인 접속 로그 확인이 필요합니다.
```

### E-08. 분석 중 다시 분석을 요청하는 경우

**상황:** 첫 번째 분석이 완료되기 전에 사용자가 분석 버튼을 다시 클릭한다.

**처리:**

- 분석 중에는 `로그 분석하기` 버튼을 비활성화한다.
- 기존 요청이 완료되거나 실패한 후 버튼을 다시 활성화한다.
- 중복 AI 요청이 발생하지 않도록 한다.

### E-09. 사용자가 초기화를 누르는 경우

**상황:** 사용자가 입력한 로그와 분석 결과를 삭제하려고 한다.

**처리:**

- 입력 영역을 빈 상태로 변경한다.
- 분석 결과를 초기 상태로 변경한다.
- 입력 줄 수를 `0 / 100줄`로 변경한다.
- 초기화 이후 새로운 로그를 바로 입력할 수 있도록 한다.

## 6. 완료 조건

- [ ] 단일 화면에서 로그 입력부터 결과 확인까지 가능하다.
- [ ] 최대 100줄의 보안 로그를 입력할 수 있다.
- [ ] 현재 입력된 로그의 줄 수가 표시된다.
- [ ] 100줄을 초과하는 입력이 제한된다.
- [ ] 로그를 입력하고 분석 버튼을 클릭할 수 있다.
- [ ] AI 분석 결과에 공격 유형이 표시된다.
- [ ] AI 분석 결과에 위험도가 표시된다.
- [ ] AI 분석 결과에 분석 설명이 표시된다.
- [ ] 분석 중 로딩 상태가 표시된다.
- [ ] 빈 입력에 대한 예외 처리가 동작한다.
- [ ] 100줄 초과 입력에 대한 예외 처리가 동작한다.
- [ ] AI API 오류에 대한 예외 처리가 동작한다.
- [ ] 네트워크 오류에 대한 예외 처리가 동작한다.
- [ ] AI 응답 형식 오류에 대한 예외 처리가 동작한다.
- [ ] 정상 로그를 입력했을 때 정상 요청으로 처리할 수 있다.
- [ ] 판단하기 어려운 로그를 입력했을 때 판단 불가로 처리할 수 있다.
- [ ] 분석 중 중복 요청이 발생하지 않는다.
- [ ] 초기화 후 새로운 로그를 다시 분석할 수 있다.
- [ ] 로그인·결제·DB 기능이 포함되지 않는다.
- [ ] 정상·공격 의심·위험 공격을 포함한 3개 이상의 테스트 케이스가 정상적으로 동작한다.
- [ ] 각 테스트에서 로그 입력부터 결과 확인까지 1분 이내에 완료할 수 있다.

## 7. 제외 범위

- 로그인 및 회원가입
- 결제 기능
- 데이터베이스 및 분석 기록 저장
- 실시간 서버·방화벽 로그 수집
- ELK, SIEM 등 외부 보안 시스템 연동
- IP 자동 차단 또는 보안 조치 자동 수행
- 모든 로그 형식 지원
- AI 분석 결과를 실제 공격으로 확정하는 기능

## 8. 제품 원칙

> AI 분석 결과는 보안 학습과 1차 판단을 돕는 참고 정보다. 실제 공격 판정과 대응 조치는 추가 로그 및 보안 담당자의 검증 후 수행해야 한다.